namespace SharpenUp.Models
{
    /// <summary>
    /// Exists only for JSON responses to show if any records are returned or not.
    /// </summary>
    public enum Status
    {
        ok,
        fail
    }
}
